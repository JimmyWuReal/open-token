# Open Token

Local-first token and cost analytics demo for AI-heavy development workflows.

This quick demo is fully local. It uses seeded browser data and `localStorage`; there is no Convex backend, auth, cloud sync, or database.

## Run

```bash
npm install
npm run dev
```

Open the local Vite URL printed in the terminal.

## Build

```bash
npm run build
```
